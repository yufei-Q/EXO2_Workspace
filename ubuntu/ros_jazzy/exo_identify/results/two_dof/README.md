# Historical two-DOF results

This directory archives outputs produced by the former two-joint test mechanism.
They are retained for traceability only and are not compatible with the current
seven-joint CSV schemas, parameter files, or neural-model format.

Do not use these files as inputs to the seven-DOF pipeline. New runs write under
`results/seven_dof/` by default.
